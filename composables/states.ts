import type { interfaceCategoria, interfaceTransacao } from "~/interfaces/interfaces"

export const msgErro = () => useState('msgErro', () => '')

// export const transacoesExistentes = () => useState<interfaceTransacao[]>('transacao', () => []);

// export const categoriasExistentes = () => useState<interfaceCategoria[]>('categoria', () => []);

// procurar sobre store